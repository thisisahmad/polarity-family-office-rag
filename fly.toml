app = "polarity-family-office-rag"
primary_region = "iad"

[build]

[env]
  PORT = "8000"
  CORS_ORIGINS = "https://your-app.vercel.app,http://localhost:3000,http://127.0.0.1:3000"

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = false
  min_machines_running = 1

  [[http_service.checks]]
    grace_period = "10s"
    interval = "30s"
    method = "GET"
    path = "/health"
    timeout = "5s"

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1
