# Web service (Render) — keep minimal for fast free-tier builds
fastapi
uvicorn[standard]
psycopg2-binary
python-dotenv
requests

# Offline-only deps (discovery/enrich CLI) — NOT imported by src.rag.main:
#   dnspython  — SMTP MX probes in enrich.py
#   serper     — via requests + SERPER_API_KEY in discovery scripts
